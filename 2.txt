Vue Directive

1. v- 접두어가 붙는 특수한 HTML 속성, 이를 통해서 추가 기능을 제공합니다. 
2. Vue 인스턴스에 연결하여 동적인 반응형 UI를 만듭니다.
3. 반응형 페이지를 만들때 
   바닐라 자바스크립트 방식에 비해서 훨씬 쉽고 코드 사용이 줄어듭니다.


<div v-bind:[attribute]="[Vue data]"></div>